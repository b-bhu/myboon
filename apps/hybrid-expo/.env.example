EXPO_PUBLIC_API_BASE_URL=http://localhost:3000
EXPO_PUBLIC_JUP_API_KEY=
EXPO_PUBLIC_SOLANA_RPC_URL=
# Polling transaction receipts for Polymarket deposit-wallet deployment. Defaults to
# the public polygon-rpc.com if unset.
EXPO_PUBLIC_POLYGON_RPC_URL=
# Override the CLOB host predict.signing.ts talks to (both @polymarket/client and
# the legacy ClobClient). Defaults to this app's own /clob proxy.
EXPO_PUBLIC_CLOB_HOST=
# Override the relayer host @polymarket/client's SecureClient talks to (e.g.
# GET /deployed). Defaults to this app's own /clob/relayer-proxy proxy.
EXPO_PUBLIC_RELAYER_HOST=
# Set to "testnet" to point Pacific perps at test-api.pacifica.fi (no real funds)
EXPO_PUBLIC_PACIFIC_ENV=mainnet
# Pacific builder code for fee sharing (register at pacifica.fi)
EXPO_PUBLIC_PACIFIC_BUILDER_CODE=MYB00N

# Turnkey (embedded Polygon wallet for Polymarket)
EXPO_PUBLIC_TURNKEY_ORG_ID=
EXPO_PUBLIC_TURNKEY_AUTH_PROXY_CONFIG_ID=

# Privy (native only — @privy-io/expo does not run in a browser)
EXPO_PUBLIC_PRIVY_APP_ID=
EXPO_PUBLIC_PRIVY_CLIENT_ID=
