# @polymarket/builder-relayer-client requires tslib at runtime but does not
# declare it as a dependency, so pnpm's strict node_modules layout leaves it
# unresolvable and the API fails to boot with MODULE_NOT_FOUND. Hoisting tslib
# to the root satisfies the undeclared require without loosening isolation for
# anything else.
public-hoist-pattern[]=tslib
